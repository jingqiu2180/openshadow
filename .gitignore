node_modules/
dist/
dist-server/
.cache/
.env
*.log
.DS_Store

# User-specific config (workspace paths, etc.)
config.json

# Stage 1a transient: .cjs mirror of test-stage1a.ts (kept for diff, not for running)
test-stage1a.cjs

# WorkBuddy / CodeBuddy internal files
.workbuddy/
.codebuddy/

# Runtime data (database, sessions, memory)
data/
agents/*/memory/
*.db-shm
*.db-wal

# Test scripts (transient)
test-*.ts
e2e-*.ts

# shared/ is now part of the core (shared between openhanako server/desktop).
# Do NOT ignore it.
nul
desktop/dist-renderer/
playwright-report/
test-results/
agents/*/sessions/
checkpoints/
node_modules/
dist/

# Build output (installer .exe, packaged app.asar, win-unpacked) — never commit.
# Accidentally committing these bloated the repo by ~1.2 GB (see v0.3.43).
release/
